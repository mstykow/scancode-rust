{
  "name": "scancode-rust",
  "version": "0.1.0",
  "private": true,
  "description": "High-performance Rust tool for detecting licenses, copyrights, and package metadata",
  "scripts": {
    "format": "prettier --write \"**/*.{yml,yaml,json,md}\"",
    "format:check": "prettier --check \"**/*.{yml,yaml,json,md}\"",
    "lint:md": "markdownlint-cli2",
    "lint:md:fix": "markdownlint-cli2 --fix \"**/*.md\"",
    "lint:docs": "npm run lint:md && npm run format:check",
    "fix:docs": "npm run lint:md:fix && npm run format",
    "check:docs": "npm run lint:docs"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "markdownlint-cli2": "0.21.0",
    "prettier": "3.8.1"
  }
}
