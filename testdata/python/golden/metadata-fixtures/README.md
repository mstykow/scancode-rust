# Python Metadata Fixtures

This directory contains local copies of Python package metadata fixtures used by parser tests.

The runtime tests in this repository must read fixture data only from `testdata/`, not from `reference/scancode-toolkit/`.

Original source: `reference/scancode-toolkit/tests/packagedcode/data/pypi/`
