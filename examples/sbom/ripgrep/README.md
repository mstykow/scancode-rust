# `ripgrep` SBOM example

SPDX tag-value ([`sbom.spdx`](sbom.spdx)) and CycloneDX JSON ([`sbom.cdx.json`](sbom.cdx.json)) software bills of materials produced by [Provenant](https://github.com/getprovenant/provenant) for [ripgrep](https://github.com/BurntSushi/ripgrep) (Rust / Cargo).

## Provenance

- Source: `https://github.com/BurntSushi/ripgrep.git` at tag `15.2.0`
- Pinned commit: `e89fff89ac9af12e8d4ce9d5fd07beb408ca730f`
- Generated by: Provenant 1.0.5
- Command: `provenant scan <checkout> --license --package --copyright --strip-root --spdx-tv sbom.spdx --cyclonedx sbom.cdx.json`

## What's inside

- **107 components** — the project plus every resolved dependency, each with a package URL (purl).
- **135 dependency relationships**, fully resolved: every `dependsOn` edge points to a component in this same document (no dangling references). A packages-only SBOM lists just the top-level package(s).

## How it compares

Verified against ScanCode with the `compare-outputs` xtask; no medium-or-major license, copyright, or package regression. Notable source-faithful differences:

- Complete inventory: every resolved dependency is promoted to a component with a purl and the dependency graph is closed, so the SBOM never references materials it does not also list.
- Cleaner copyright: Provenant does not record two prose fragments that ScanCode captures as "authors" (`missed. Similarly`; a bare `github.com/Genivia/ugrep` URL).

See [`../README.md`](../README.md) for the full target list and the verification method.
