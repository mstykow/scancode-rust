# `express` SBOM example

SPDX tag-value ([`sbom.spdx`](sbom.spdx)) and CycloneDX JSON ([`sbom.cdx.json`](sbom.cdx.json)) software bills of materials produced by [Provenant](https://github.com/getprovenant/provenant) for [express](https://github.com/expressjs/express) (JavaScript / npm).

## Provenance

- Source: `https://github.com/expressjs/express.git` at tag `v5.2.1`
- Pinned commit: `dbac741a49a5a64336b70c06e85c2e2706e36336`
- Generated by: Provenant 1.0.5
- Command: `provenant scan <checkout> --license --package --copyright --strip-root --spdx-tv sbom.spdx --cyclonedx sbom.cdx.json`

## What's inside

- **45 components** — the project plus every resolved dependency, each with a package URL (purl).
- **44 dependency relationships**, fully resolved: every `dependsOn` edge points to a component in this same document (no dangling references). A packages-only SBOM lists just the top-level package(s).

## How it compares

Verified against ScanCode with the `compare-outputs` xtask; no medium-or-major license, copyright, or package regression. Notable source-faithful differences:

- Complete inventory: every resolved dependency is promoted to a component with a purl and the dependency graph is closed, so the SBOM never references materials it does not also list.
- Source-faithful copyright: express source literally writes `Copyright(c)` (no space) and Provenant preserves it, where ScanCode rewrites it to `Copyright (c)`.
- Cleaner authors: Provenant extracts `TJ Holowaychuk <tj@vision-media.ca>` and drops three ScanCode false-positive "authors" (a `graphs/contributors` URL and an `application.js` filename).

See [`../README.md`](../README.md) for the full target list and the verification method.
